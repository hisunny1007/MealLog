<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yunyun.meallog.food.dao.FoodDao">

    <!-- 자동완성: 앞글자로 빠르게 검색 -->
<!--    김치를 검색했는데 가자미튀김이 나옴. 정렬 기준이 없어서 추가 필요!-->
<!--    정확히 일치, 시작 일치, 중간 포함, 길이 짧은 순-->
    <select id="autoComplete" parameterType="string" resultType="FoodAutoCompleteResponseDto">
            SELECT MIN(id) AS id, name
            FROM foods
            WHERE name LIKE CONCAT('%', #{query}, '%')
            GROUP BY name
            ORDER BY
                CASE
                    WHEN name = #{query} THEN 0
                    WHEN name LIKE CONCAT(#{query}, '%') THEN 1
                    ELSE 2
                    END,
                LENGTH(name),
                name
                LIMIT 10
        </select>

        <!-- 일반 검색 -->
        <select id="searchFood" parameterType="string" resultType="Food">
            SELECT *
            FROM foods
            WHERE name LIKE CONCAT('%', #{query}, '%')
            ORDER BY LENGTH(name), name
        </select>

        <!-- 상세 조회 -->
        <select id="getFoodDetail" parameterType="long" resultType="Food">
            SELECT *
            FROM foods
            WHERE id = #{id}
        </select>

        <!-- 커스텀 음식 등록 -->
        <insert id="insertCustomFood" parameterType="Food" useGeneratedKeys="true" keyProperty="id">
            INSERT INTO foods (name, calories, carbs, protein, fat, is_custom, created_by, created_at)
            VALUES (#{name}, #{calories}, #{carbs}, #{protein}, #{fat},#{isCustom}, #{createdBy}, #{createdAt})
        </insert>

</mapper>
